apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flkafka.fullname" . }}-test-connection"
  labels:
    {{- include "flkafka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flkafka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
